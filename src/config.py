# project configuration file
import os
from os.path import join

project_root_path = '/home/lara/Documents/Work/Papers/BASE_NeuroImage(published)/repo'
pytorch_models_path = join(project_root_path, 'BASE_models')
model_predictions_path = join(project_root_path, 'predictions')
